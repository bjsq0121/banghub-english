# Production build: API is served same-origin via Firebase Hosting
# rewrites (/api/** -> Cloud Run service). Empty base means fetch() uses
# the current origin, so cookies ride along without CORS gymnastics.
VITE_API_BASE_URL=
