rules_version = '2';

// Backend uses firebase-admin which bypasses these rules.
// All user traffic routes through the Fastify API; never direct client access.
// Default-deny is the safe posture.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
